
Proc e9(G:Graph, A,B: N_P<Int>(G)) : Int
{
    Int z=0;

    // Return inside Foreach
    Foreach (n: G.Nodes) { 
        Foreach (k: n.Nbrs) { 
            Return z;
        }
    }

    Return z;
}
