Procedure triangle2(G: Graph): Int
{
	Int T=0;

	Foreach(u: G.Nodes) {
        Foreach(w: u.Nbrs) (w > u) {
    	   Foreach(v: u.InNbrs) (u > v) 
                If (v.HasEdgeTo(w)) {
                    T++;
            }
        }
    }

   	Return T;   
}
