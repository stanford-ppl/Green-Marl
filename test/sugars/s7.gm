// Test of Count <-> Foreach
// Test of Auto bound
Proc Foo(G:Graph, A:N_P<Bool>(G), B:N_P<Int>(G))
{
    Int y,y2;
    y = Count(t:G.Nodes)(t.A);

    Foreach(t:G.Nodes)(t.B>0) {
        y2++;             
    }
}
