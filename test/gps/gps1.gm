
Procedure gps_test (G: Graph, x: Int, A:Node_Property<Int>(G) ) : Int
{
    Int z = 0;
    If (x > 0) {
        // Test of reduction into z
        z = Sum(t: G.Nodes) {t.A};
    }

    // Test of Returen
    Return z;
}
