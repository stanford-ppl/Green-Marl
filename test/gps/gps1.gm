
// Test of basic structure creation
Procedure gps1 (G: Graph, x: Int, A,B:Node_Property<Int>(G) ) : Int
{
    Int z = 0;
    If (x > 0) {
        // Test of reduction into z
        z = Sum(t: G.Nodes) {t.A + t.B};
    }

    // Test of Return
    Return z;
}
