// Test of CommonNbr
Proc b10(G: Graph, A,B: N_P<Int>(G), C:E_P<Int>(G), s: Node(G))
{
    Foreach(t:G.Nodes)(t.A > 0) {
        Foreach(n: t.CommonNbrs(s)) {
            n.B += t.A @ t;
        }
    }
}
