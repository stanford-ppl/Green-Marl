Procedure test_property_of_collection_iterate(G:Graph)
{
    /* This syntax has been deprecated
    N_P<N_S> prop;
    Node n;
    
    For(s: prop.Items)
    	s.Add(n);
    	
    Foreach(s: prop.Items)
    	s.Add(n);
    */
}
