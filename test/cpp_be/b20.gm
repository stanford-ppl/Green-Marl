Procedure b20_test_default_graph(G:Graph)
{
    //must compile since only one graph is present
    Node n;
    Edge e;

    Node_Property<Bool> np;
    Edge_Property<Bool> ep;

    n.np = True;
    e.ep = False;
}
