
// Test of temporary property declaration
// Test of temporary property deletation
Proc P(G: Graph, A:N_P<Int>(G), b:Int)
{
    N_P<Int>(G) B;

    If (b>0) 
    {
        N_P<Int>(G) C;

        If (b>1) 
        {
            N_P<Int>(G) D;
        }
    }
}

Proc Q(G: Graph, A:N_P<Int>(G), b:Int)
{
    N_P<Float>(G) B;

    Return;
}
