Procedure b29_test_property_of_collection_instant_assign(G:Graph)
{
    N_P<N_S> prop;
    Node n;
    Bool b;
    
    N_S set = n.prop;	//do not create an empty set when declaring the set
    
    If(b)
  		N_S s3 = n.prop;	//do not create an empty set when declaring the set
  	Else
  		N_S s3 = n.prop;	//do not create an empty set when declaring the set
  		
  	For(x: G.Nodes) {
  		N_S s = x.prop;	//do not create an empty set when declaring the set
  	}
}
