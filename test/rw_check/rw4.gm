
// Warning
Proc P(G: Graph, A,B,C:N_P<Int>(G))
{
    Foreach(t: G.Nodes) {
        Foreach(u: t.Nbrs) {
            t.C += u.A;  // read A random
        }
        t.A = t.B + 3; // write A linear
    }
}
